package testesAula;

public class Anotacoes {
	
	

	// colocar main ctrl espaço para aparecer o método

	// Syso ctrl espaço para aparecer o println

	/*
	 * colocar "obj.close();", para tirar aquela atenção "!" do programa e serve
	 * para a segurança também.
	 */

	/*
	 * println é para pular uma linha print não pula linha
	 */

	/*
	 * Float suporta até 6 ou 7 casas decimais e o double 15. Por isso as vezes é
	 * necessário trocar o float por double
	 */

	/*
	 * Apertando ctrol N você consegue abrir uma nova classe apenas digitando
	 */

	/*
	 * System.out.printf("A divisão dos números informados é: %.2f ", divisao);
	 * colocar o f no fim do print para puxarmos o float no %.2f (define a
	 * quantidade de casas decimais).
	 */

	/*
	 * Quando concatenar concatenar com ",", tem que usar o printf. Quando
	 * concatenar com "+" tem que usar o print ou println
	 */

}
